import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devynsharpe.googlelogintest',
  appName: 'GoogleLoginTest',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
